# ==============================
# A) 本地脚本模式专用（可选）
# 仅当你在本地执行 `npm run imgbed:generate-index` 才需要填写
# ==============================
IMGBED_BASE_URL=https://img.example.com
IMGBED_API_TOKEN=your_api_token
IMGBED_LIST_ENDPOINT=/api/manage/list
IMGBED_RANDOM_ENDPOINT=/random
IMGBED_FILE_ROUTE_PREFIX=/file
IMGBED_LIST_DIR=
IMGBED_PREVIEW_DIR=0_preview
IMGBED_LIST_RECURSIVE=true
IMGBED_ENABLE_PREVIEW_MAPPING=true
IMGBED_PREVIEW_FALLBACK_TO_ORIGINAL=true
IMGBED_PAGE_SIZE=200
IMGBED_DEFAULT_CATEGORY=uncategorized

# ==============================
# B) 管理后台与远端配置（/admin）
# 推荐部署到 Cloudflare Pages 后直接在 WebUI 配置
# ==============================
# 管理员账号密码（默认 admin / admin，强烈建议修改）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
# 可选：使用 SHA256 密码（填了会优先于 ADMIN_PASSWORD）
# ADMIN_PASSWORD_SHA256=
ADMIN_SESSION_SECRET=change-this-secret
ADMIN_SESSION_HOURS=24

# 配置存储后端：d1 或 kv（不填则自动检测绑定）
CONFIG_STORE_DRIVER=d1

# 默认站点配置（当域名未写入存储时使用）
DEFAULT_GALLERY_DATA_MODE=static
DEFAULT_DISPLAY_MODE=fullscreen
DEFAULT_SHUFFLE_ENABLED=true
DEFAULT_GALLERY_INDEX_URL=
DEFAULT_SITE_TITLE=Gallery-Portfolio
DEFAULT_SITE_IMAGE_URL=
DEFAULT_IMGBED_BASE_URL=
DEFAULT_IMGBED_API_TOKEN=
DEFAULT_IMGBED_LIST_ENDPOINT=/api/manage/list
DEFAULT_IMGBED_RANDOM_ENDPOINT=/random
DEFAULT_IMGBED_RANDOM_ORIENTATION=
DEFAULT_IMGBED_FILE_ROUTE_PREFIX=/file
DEFAULT_IMGBED_LIST_DIR=
DEFAULT_IMGBED_PREVIEW_DIR=0_preview
DEFAULT_IMGBED_RECURSIVE=true
DEFAULT_IMGBED_PAGE_SIZE=200
